const EditDataSourceStore = require('../../../src/web/data-sources/EditDataSourceStore');

describe('EditDataSourceStore', () => {
    it('should exist', () => {
        expect(EditDataSourceStore).toBeTruthy();
    });
});
