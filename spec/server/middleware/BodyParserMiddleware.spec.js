const BodyParserMiddleware = require('../../../src/server/middleware/BodyParserMiddleware');

describe('BodyParserMiddleware', () => {
    it('should exist', () => {
        expect(BodyParserMiddleware).toBeTruthy();
    });
});
